{
  "name": "movingobjects-io",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "clean": "rm -rf .nuxt",
    "dev:clean": "rm -rf .nuxt && nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "favicons": "node scripts/generate-favicons.js",
    "prebuild": "npm run favicons",
    "pregenerate": "npm run favicons"
  },
  "dependencies": {
    "nuxt": "^4.2.1",
    "vue": "^3.5.24",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "favicons": "^7.2.0",
    "sass": "^1.94.2"
  }
}
